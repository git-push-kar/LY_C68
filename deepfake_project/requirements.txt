# requirements.txt
# ================
# Dependencies for DeepfakeReasoningModel v2 (InternVL3-2B + LoRA + PyTorch)

# PyTorch with CUDA 12.6 (verified on RTX A5000, driver CUDA 13.2).
# Pins mirror the working venv exactly — do NOT unpin without re-running
# the model-build smoke test (transformers 5.x breaks model load).
--extra-index-url https://download.pytorch.org/whl/cu126

torch==2.6.0
torchvision==0.21.0
# NOTE: torchaudio intentionally omitted — unused by the v2 pipeline.

# Hugging Face & Fine-tuning
# transformers pinned <5: the custom modeling_internvl_chat.py targets the
# 4.x API (_tied_weights_keys); 5.x renames it (all_tied_weights_keys) and
# crashes model load. Verified working set: transformers 4.37.2 + peft 0.7.1.
transformers==4.37.2
peft==0.7.1
accelerate==1.14.0
safetensors>=0.4.0
einops>=0.7.0
timm>=0.9.0
tokenizers>=0.15.0

# Scientific & Visualization
numpy>=1.24.0,<2.3.0
Pillow>=10.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
PyYAML>=6.0
tqdm>=4.65.0
scipy>=1.11.0